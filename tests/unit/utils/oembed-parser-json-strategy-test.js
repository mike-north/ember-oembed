import oembedParserJsonStrategy from 'dummy/utils/oembed-parser-json-strategy';
import { module, test } from 'qunit';

module('Unit | Utility | oembed parser json strategy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oembedParserJsonStrategy();
  assert.ok(result);
});
