import oembedParser from 'dummy/utils/oembed-parser';
import { module, test } from 'qunit';

module('Unit | Utility | oembed parser');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oembedParser();
  assert.ok(result);
});
