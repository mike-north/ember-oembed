import oembedParserXmlStrategy from 'dummy/utils/oembed-parser-xml-strategy';
import { module, test } from 'qunit';

module('Unit | Utility | oembed parser xml strategy');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oembedParserXmlStrategy();
  assert.ok(result);
});
